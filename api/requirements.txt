# Only what the gateway imports. The package itself is bundled from the repository (see vercel.json
# includeFiles), not installed, so the training stack -- torch, transformers, pyarrow, datasets, scipy,
# scikit-learn, litellm -- never reaches the function. That is the difference between an 84 MB bundle and one
# that does not fit inside Vercel's 250 MB limit at all.
fastapi>=0.115
pydantic>=2
sqlalchemy>=2
pyyaml>=6
numpy>=1.26
httpx>=0.27
jsonschema>=4.21
