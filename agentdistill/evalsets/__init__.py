"""Generated eval sets.

Eval sets that are carved out of a larger corpus (the tiny rehearsal's ten- and twenty-task sets) are pinned by
content, not by whatever order the corpus happened to be written in. See `agentdistill.evalsets.generate`.

Nothing is re-exported here: `python -m agentdistill.evalsets.generate` is the entry point the setup script
runs, and importing the module from the package first makes runpy warn about executing it twice.
"""
